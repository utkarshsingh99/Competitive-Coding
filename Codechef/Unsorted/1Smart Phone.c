#include <stdio.h>
int main()
{
  typedef long long int ll;
  ll n,a[1000000];
  scanf("%lld",&n);
  for(ll i=0;i<n;i++)
  {
    scanf("%lld",&a[i]);
  }
  for(ll i=0;i<n;i++)
  {
    for(ll j=0;j<n;j++)
    {
      if(a[i]<=a[j])
      {
        
      }
    }
  }
}
